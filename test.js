const colors = require("kleur/colors");

console.log(
  colors.dim("[test.js]"),
  "Test colored output:",
  colors.yellow("Yellow"),
  colors.cyan("Cyan"),
  colors.green("Green"),
  colors.red("Red")
);
